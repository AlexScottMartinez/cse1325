using namespace std;

class Tile {
	string _word;
	bool _matched;
	static int _width = -1;
	static string _blank;

Public:
	Tile(word: string) {
        
    }
	bool match(tile: Tile&) {
        if(_word == _word)
            _matched = true;
            return _matched;
    }
	string word() {
        return (_word);
    }
	bool matched() {
        return (_matched);
    }
	static int width() {
        return (_width);
    }
	string to_string() {
        if(_matched == true){
            return (_word);
        }
        else {
            return (_blank);
        }        
    }
}
